 OCSALY
 Memory and STL
#include array
#include iostream

using namespace std;

void use_ten_inits(int );

int main() {
     out_of_range
    array and vector - STANDART LIBRARY ARRAYS
    arrayint, 4 arr {1, 2, 3, 4};

    for (int i = 0; i  arr.size(); ++i) {
        cout  arr[10]  endl;
    }

    int arr1[]{1, 2, 3, 4};

    use_ten_inits(&arr1[0]);
    use_ten_inits(arr1.data());


}
